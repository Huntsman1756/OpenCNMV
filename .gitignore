# Transient probe artifacts (regenerated each session)
g0-r/_probe-logs/tmp-nif-*
g0-r/_probe-logs/cj-*
g0-r/_probe-logs/sample-guid.txt
g0-r/_probe-logs/gleif-*.json
g0-r/_probe-logs/tmp-*.bin
g0-r/_probe-logs/*.cookies

# OS / editor
Thumbs.db
.DS_Store

# R11 brel oracle venv (not part of evidence)
g0-r/R11-esef-arelle-parse/.venv-brel/

# Python
__pycache__/
*.pyc

# R12 temp entrypoint copies (byte-identical, deleted per run)
g0-r/R12-ipp-arelle-parse/evidence/_entrypoints/

# R14 run roots (transient; canonical projections copied to evidence/)
g0-r/R14-online-capture-deterministic/_runs/

# R15 run roots
g0-r/R15-offline-rebuild-deterministic/_runs/

# G1-B run roots (transient; canonical evidence committed under evidence/)
g1/G1-B-dual-language-capture/_runs/
g1/G1-C-extension-variant-identity/_runs/

# G2-A run roots (transient; rebuild output compared byte-for-byte)
g2/G2-A-durable-canonical-core/_out/
g2/G2-B-frozen-corpus-rebuild/_out/
