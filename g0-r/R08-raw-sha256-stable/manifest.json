{
  "gate": "R8",
  "name": "RAW_SHA256_STABLE",
  "status": "PASS",
  "executed_at": "2026-09-14T22:15:14Z",
  "code_commit": "n/a",
  "inputs": [
    "g0-r/R07-raw-retrieval/artifact_manifest.json",
    "27 artefacts (IPP re-resolved via nreg -> fresh ?t={GUID}; ESEF via stable ver?e=<token>)"
  ],
  "outputs": [
    "g0-r/R08-raw-sha256-stable/README.md",
    "g0-r/R08-raw-sha256-stable/evidence/sha256_verify.json"
  ],
  "sha256": {
    "sha256_verify.json": "see evidence/sha256_verify.json"
  },
  "findings": [
    "REMEDIATED: verification now runs on the complete corpus inventory.",
    "27/27 artefacts byte-stable on re-download (0 mismatch, 0 errors): 15 IPP + 6 ESEF_COVER + 6 ESEF_PACKAGE_ZIP_XBRL.",
    "The 6 ESEF IXBRL_CONSOLIDATED (real inline-XBRL) were separately verified byte-stable out-of-session (run1==run2) in the R4 remediation.",
    "IMPORTANT: the IPP ?t={GUID} is ephemeral; re-downloading a stored ?t= URL returned EMPTY. IPP must be re-resolved via nreg -> detail -> fresh GUID (discovery) for stable re-download. This confirms R6 (nreg is the stable locator, not the URL).",
    "No source drift detected in this window."
  ],
  "limitations": [
    "Stability verified within one session window; long-horizon drift monitored by re-running this verification periodically (a changed hash means the source changed -> flag, never overwrite)."
  ]
}
