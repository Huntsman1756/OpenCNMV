"""Read-only query surface over a materialized COLUMNAR_DATASET_V1.

All public-CLI domain semantics live here; ``opencnmv.cli`` is a thin
presentation layer over these functions. Nothing in this package writes
to the dataset or performs network access.
"""
