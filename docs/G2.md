# G2 — durable core (preregistered)

**Status:** G2-A `DURABLE_CANONICAL_CORE` **PASS** (6/6) — `src/opencnmv/`
rebuilds the four frozen fixtures byte-identically, offline, pinned inputs,
no gate-code imports.

G1 is closed (`g1-model-frozen`). G2 turns the frozen contract into
maintainable software **without degrading the demonstrated invariants**.
The frozen schema (`g1/G1-E-canonical-model-freeze/canonical_model_v1.schema.json`)
is the contract; the durable model must validate against it, and any
incompatible change requires an explicit `CANONICAL_MODEL_V2`.

## Gates (preregistered)

```text
G2-A  DURABLE_CANONICAL_CORE
      production code (src/opencnmv/) rebuilds the four frozen G1-E
      fixtures from preserved evidence — semantically identical objects,
      byte-identical where the serializer is deterministic.
      Hard rules: 0 imports from g0-r/ and g1/, 0 network for fixture
      rebuild, 0 hidden Arelle cache dependencies.

G2-B  FULL_FROZEN_CORPUS_REBUILD
      the durable core rebuilds the whole frozen corpus (ESEF FY2024/25
      dual variants + IPP slots) offline from preserved raw bytes.

G2-C  COLUMNAR_DATASET_V1
      fact dataset to Parquet/DuckDB. Entity/model layer (filing,
      variant, variant_version, version_event, mapping) stays in small
      relational tables — NOT flattened into the fact table.

G2-D  INCREMENTAL_CAPTURE / UPDATE SEMANTICS
      version_event-driven updates producing new variant_versions.
```

CLI/API only after G2-C.

## Module layout (G2-A)

```text
src/opencnmv/
  model/         canonical.py (V1-conformant objects), ids.py
  source/cnmv/   discovery.py, retrieval.py   (official surfaces only)
  xbrl/          arelle.py (thin adapter), taxonomy.py
  canonicalize/  filing.py, variants.py, facts.py, events.py,
                 extension_mapping.py
  provenance/    hashes.py, manifests.py
  serialize.py   deterministic canonical JSON
```

## G2-A acceptance (preregistered)

```text
IBE FY2024   2 UI views / 1 submission_variant / FALLBACK_TO_ES
BBVA FY2024  2 variants; divergent Equity stays +98M / -98M
SAN FY2024   only PROVEN extension mappings rewrite identity
TEF 20484    es 1 version; en v1(unobserved)->v2(observed);
             13/03 affects only #en; source_nreg null;
             28/02 VARIANT_SCOPE_NOT_OBSERVABLE

PASS condition: production output == frozen G1-E fixtures
(semantic equality; byte equality where serialization is deterministic)
```
